{
  "ParamType": {
    "name": "type",
    "in": "query",
    "required": true,
    "schema": {
      "type": "string",
      "enum": ["numeric", "enum"]
    },
    "examples": {
      "numeric": {
        "summary": "Numeric type",
        "value": "numeric"
      },
      "enum": {
        "summary": "Enum type",
        "value": "enum"
      }
    }
  }
}
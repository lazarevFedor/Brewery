{
  "CategoryParamsApply": {
    "type": "object",
    "properties": {
      "numeric_param_ids": {
        "type": "array",
        "items": { "type": "integer" },
        "description": "List of numeric parameter ids to apply"
      },
      "enum_param_ids": {
        "type": "array",
        "items": { "type": "integer" },
        "description": "List of enum parameter ids to apply"
      }
    }
  }
}